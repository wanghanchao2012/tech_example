<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.multids.mapper.UserMapper">
	<select id="queryMoney" resultType="com.example.multids.entity.Money">
  		select * from money
  </select>
	
	<select id="queryUser" resultType="com.example.multids.entity.User">
  		select * from user
  </select>
</mapper>